---
title: "Predict House Price"
author: "Ronald Bailey"
date: "`r Sys.Date()`"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
system("git pull")#este actualiza el repositorio
```


```{r}
system("git pull")#este actualiza el repositorio
system("git add .")
system("git commit -m \"Primer commit\" ")
system("git push")
```
```{r}
system("git pull")#este actualiza el repositorio
system("git add .")
system("git commit -m \"Carga de datos\" ")
system("git push")
```

